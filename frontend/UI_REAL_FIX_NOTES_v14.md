# v14 Real Fix Notes

这版不是继续改文案说明，而是实际落代码修两个问题：

1. 搜索结果过多时，搜索页改成前端分页显示，包含：
   - 自适应每页条数
   - 页码切换
   - 上一页 / 下一页
   - 已显示进度条
2. 详情打开索引按分页偏移修正，避免翻页后点开详情对应错位。

另外顺手加了一层 1080p 下的响应式优化：
- 搜索页右侧栏在中等宽度下改为双列 / 下沉，减少拥挤
- 每页条数会随视图宽度和结果视图模式动态变化
