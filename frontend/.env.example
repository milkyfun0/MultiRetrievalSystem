# Vite dev server 反向代理目标（npm run dev 时生效）
# 本机调试: http://127.0.0.1:8000
# 局域网部署: http://<LAN_HOST>:8000
VITE_BACKEND_PROXY_TARGET=http://127.0.0.1:8000

# axios 直连后端的 baseURL；留空则走相对路径 '/'（依赖 Vite 代理或同源部署）
# 局域网部署时建议显式填: http://<LAN_HOST>:8000
VITE_API_BASE_URL=